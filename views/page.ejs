<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css"> <!-- load bootstrap css -->    
    </head>
    <body>
        <h1><%= title %></h1>
        <a href="/board/write">글쓰기</a>
        <a href="/room_list">뒤로가기</a>
        
        <table  class = "table table-striped table-bordered table-hover", style = "width: 80%; text-align: center; margin: auto;">
            <tr>
                <td>번호</td>
                <td>이메일</td>
                <td>제목</td>
                <td>등록일</td>
            </tr>
            <%
            for(var i =(page * page_num) - page_num; i < (page * page_num);i++){
                if(i>length){
                    i++;
                }else{
                    var data = rows[i]
                %>
            <tr>
                <td><%= i + 1 %></td>
                <td><%= data.name %></td>
                <td><a href="/board/read/<%= data.idx %>"><%= data.title %></a></td>
                <td><%=data.regdate %></td>
            </tr>                }
            <% } } %>
        <tr>
            <td colspan="6">
                <%
                for(var j = 0; j < length / page_num; j++){
                    %>
                    [<a href="/board/page/<%= j + 1 %>"><%= j + 1 %></a>]
                <%}%>
            </td>
        </tr>
        </table>
    </body>
</html>